fastapi==0.111.0
uvicorn[standard]==0.30.1
redis==5.0.4
elasticsearch==8.13.1
pydantic==2.7.1
aiohttp==3.9.5
# Pretrained NLP path (loaded lazily only if USE_PRETRAINED_NLP=true).
# torch is installed separately in the Dockerfile from the official PyTorch
# CPU wheel index — see Dockerfile for why.
transformers==4.41.2
